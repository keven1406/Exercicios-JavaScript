var arr1 = ["a","b","c","d"];
function reverseArrayinPlace(arr1){
  var local = [];
  for (var i = (arr1.length - 1); i >= 0; i--){
    arr1.push(arr1[i]);
   }
  return arr1.slice(Math.floor(arr1.length/2),arr1.length);
}
console.log(reverseArrayinPlace(arr1));

